import UIKit

var str = "Hello, playground"

func sumAndProduct() -> what exactly this challenge wants me to return i have no idea {
              var x: Int
              var y: Int
              var a = x + y
              var b:Int = x *= y
              var aB = [a, b] as [Any]
              if aB.a > aB.b {
              print("(\(aB.a), \(aB.b))")
                
                    }
              if aB.a < aB.b {
                print("(\(aB.b). aB.a))")
}
}
 
